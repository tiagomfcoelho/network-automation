- name: Provision Devices in Vaultwarden and NetBox
  hosts: localhost
  connection: local
  gather_facts: false

  vars_files:
    - "{{ playbook_dir }}/../group_vars/all.yml"

  vars:
    target_device: "all"

  tasks:

    # -------------------------------------------------------------------------
    # VALIDATION
    # -------------------------------------------------------------------------

    - name: Validate devices structure
      assert:
        that:
          - devices is defined
          - devices | length > 0

    - name: Validate required fields
      assert:
        that:
          - item.name is defined
          - item.ip is defined
          - item.site is defined
          - item.site_slug is defined
          - item.role_slug is defined
          - item.device_type is defined
          - item.manufacturer_slug is defined
          - item.username is defined
          - item.password is defined
          - item.vault_folder is defined
      loop: "{{ devices }}"
      when: target_device == 'all' or item.name == target_device

    # -------------------------------------------------------------------------
    # FILTER DEVICES (IMPORTANT)
    # -------------------------------------------------------------------------

    - name: Build filtered device list
      set_fact:
        filtered_devices: >-
          {{ devices if target_device == 'all'
             else devices | selectattr('name', 'equalto', target_device) | list }}

    - name: Fail if no devices match
      fail:
        msg: "No devices found for target_device={{ target_device }}"
      when: filtered_devices | length == 0

    # -------------------------------------------------------------------------
    # VAULTWARDEN
    # -------------------------------------------------------------------------

    - name: Get Vault folders
      uri:
        url: "{{ vault_api_url }}/vault/folders"
        method: GET
        headers:
          x-api-key: "{{ vault_token }}"
      register: vault_folders

    - name: Create Vault items (idempotent)
      uri:
        url: "{{ vault_api_url }}/vault/item"
        method: POST
        headers:
          x-api-key: "{{ vault_token }}"
          Content-Type: application/json
        body_format: json
        body:
          name: "{{ item.name }}"
          username: "{{ item.username }}"
          password: "{{ item.password }}"
          uri: "ssh://{{ item.ip }}"
          folder_id: "{{ (vault_folders.json.folders
                          | selectattr('name', 'equalto', item.vault_folder)
                          | first).id }}"
          fields:
            - { name: ip, value: "{{ item.ip | string }}" }
            - { name: port, value: "{{ (item.ssh_port | default(22)) | string }}" }
            - { name: device_type, value: "{{ item.device_type | string }}" }
            - { name: site, value: "{{ item.site | string }}" }
      loop: "{{ filtered_devices }}"
      register: vault_results
      failed_when: vault_results.status not in [200, 201, 409]  # 409 = already exists

    # -------------------------------------------------------------------------
    # NETBOX — BASE OBJECTS
    # -------------------------------------------------------------------------

    - name: Ensure sites
      netbox.netbox.netbox_site:
        netbox_url: "{{ netbox_url }}"
        netbox_token: "{{ netbox_token }}"
        data:
          name: "{{ item.site }}"
          slug: "{{ item.site_slug }}"
        state: present
      loop: "{{ filtered_devices }}"
      loop_control:
        label: "{{ item.site }}"

    - name: Ensure manufacturers
      netbox.netbox.netbox_manufacturer:
        netbox_url: "{{ netbox_url }}"
        netbox_token: "{{ netbox_token }}"
        data:
          name: "{{ item.manufacturer }}"
          slug: "{{ item.manufacturer_slug }}"
        state: present
      loop: "{{ filtered_devices }}"

    - name: Ensure device types
      netbox.netbox.netbox_device_type:
        netbox_url: "{{ netbox_url }}"
        netbox_token: "{{ netbox_token }}"
        data:
          manufacturer:
            slug: "{{ item.manufacturer_slug }}"
          model: "{{ item.device_type }}"
          slug: "{{ item.device_type }}"
        state: present
      loop: "{{ filtered_devices }}"

    - name: Ensure roles
      netbox.netbox.netbox_device_role:
        netbox_url: "{{ netbox_url }}"
        netbox_token: "{{ netbox_token }}"
        data:
          name: "{{ item.role | capitalize }}"
          slug: "{{ item.role_slug }}"
          color: "{{ role_colors[item.role] | default(role_colors['default']) }}"
        state: present
      loop: "{{ filtered_devices }}"

    # -------------------------------------------------------------------------
    # NETBOX — DEVICES
    # -------------------------------------------------------------------------

    - name: Ensure devices
      netbox.netbox.netbox_device:
        netbox_url: "{{ netbox_url }}"
        netbox_token: "{{ netbox_token }}"
        data:
          name: "{{ item.name }}"
          device_type:
            slug: "{{ item.device_type }}"
          device_role:
            slug: "{{ item.role_slug }}"
          site:
            name: "{{ item.site }}"
        state: present
      loop: "{{ filtered_devices }}"

    # -------------------------------------------------------------------------
    # INTERFACES
    # -------------------------------------------------------------------------

    - name: Ensure management interface
      netbox.netbox.netbox_device_interface:
        netbox_url: "{{ netbox_url }}"
        netbox_token: "{{ netbox_token }}"
        data:
          device:
            name: "{{ item.name }}"
          name: "{{ item.mgmt_interface | default('Management0') }}"
          type: "{{ item.interface_type | default('1000base-t') }}"
          mgmt_only: true
        state: present
      loop: "{{ filtered_devices }}"

    # -------------------------------------------------------------------------
    # IPs (IDEMPOTENTE)
    # -------------------------------------------------------------------------

    - name: Check if IP exists
      uri:
        url: "{{ netbox_url }}/api/ipam/ip-addresses/?address={{ item.ip }}"
        method: GET
        headers:
          Authorization: "Token {{ netbox_token }}"
      loop: "{{ filtered_devices }}"
      register: ip_check

    - name: Create IP if missing
      netbox.netbox.netbox_ip_address:
        netbox_url: "{{ netbox_url }}"
        netbox_token: "{{ netbox_token }}"
        data:
          address: "{{ item.item.ip }}/{{ item.item.ip_prefix | default(32) }}"
          assigned_object:
            device: "{{ item.item.name }}"
            name: "{{ item.item.mgmt_interface | default('Management0') }}"
        state: present
      loop: "{{ ip_check.results }}"
      when: item.json.count == 0

    # -------------------------------------------------------------------------
    # PRIMARY IP
    # -------------------------------------------------------------------------

    - name: Set primary IP (robust)
      netbox.netbox.netbox_device:
        netbox_url: "{{ netbox_url }}"
        netbox_token: "{{ netbox_token }}"
        data:
          name: "{{ item.item.name }}"
          primary_ip4: "{{ item.json.results[0].id }}"
        state: present
      loop: "{{ ip_check.results }}"
      when: item.json.count > 0

    # -------------------------------------------------------------------------
    # SUMMARY
    # -------------------------------------------------------------------------

    - name: Summary
      debug:
        msg: "✓ {{ item.name }} provisioned successfully"
      loop: "{{ filtered_devices }}"